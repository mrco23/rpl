generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Admin {
  id_admin Int     @id @default(autoincrement())
  username String? @unique
  password String

  profil           Profil[]
  kontak           Kontak[]
  berita           Berita[]
  pengumuman       Pengumuman[]
  fasilitas        Fasilitas[]
  program_unggulan ProgramUnggulan[]
  ekstrakurikuler  Ekstrakurikuler[]
  prestasi         Prestasi[]

  @@map("admin")
}

model Verifikator {
  id_verifikator Int    @id @default(autoincrement())
  username       String @unique
  password       String
  nama           String

  dokumen Dokumen[]

  @@map("verifikator")
}

model Profil {
  id_profil           Int    @id @default(autoincrement())
  nama_sekolah        String @unique
  visi                String @db.Text
  misi                String @db.Text
  nama_kepala_sekolah String
  foto_kepala_sekolah String
  kata_sambutan       String @db.Text
  id_admin            Int

  admin Admin @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)

  @@index([id_admin], map: "profil_id_admin_idx")
  @@map("profil")
}

model Kontak {
  id_kontak Int     @id @default(autoincrement())
  no_telpon String
  instagram String
  tiktok    String
  facebook  String
  youtube   String
  email     String
  whatsapp  String?
  id_admin  Int

  admin Admin @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)

  @@index([id_admin], map: "kontak_id_admin_idx")
  @@map("kontak")
}

model Berita {
  id_berita      Int      @id @default(autoincrement())
  judul_berita   String
  deskripsi      String   @db.Text
  gambar_berita  String?
  tanggal_dibuat DateTime @default(now())
  id_admin       Int

  admin Admin @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)

  @@index([id_admin], map: "berita_id_admin_idx")
  @@map("berita")
}

model Pengumuman {
  id_pengumuman    Int      @id @default(autoincrement())
  judul_pengumuman String
  deksripsi        String?  @db.Text
  tanggal_dibuat   DateTime @default(now())
  id_admin         Int
  id_pendaftar     Int?

  admin     Admin      @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)
  pendaftar Pendaftar? @relation(fields: [id_pendaftar], references: [id_pendaftar], onDelete: Cascade)

  @@index([id_admin], map: "pengumuman_id_admin_idx")
  @@index([id_pendaftar], map: "pengumuman_id_pendaftar_idx")
  @@map("pengumuman")
}

model Fasilitas {
  id_fasilitas     Int     @id @default(autoincrement())
  nama_fasilitas   String
  deskripsi        String  @db.Text
  gambar_fasilitas String?
  id_admin         Int

  admin Admin @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)

  @@index([id_admin], map: "fasilitas_id_admin_idx")
  @@map("fasilitas")
}

model ProgramUnggulan {
  id_program Int     @id @default(autoincrement())
  nama       String
  deskripsi  String  @db.Text
  gambar     String?
  id_admin   Int

  admin Admin @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)

  @@index([id_admin], map: "program_unggulan_id_admin_idx")
  @@map("program_unggulan")
}

model Ekstrakurikuler {
  id_ekstrakurikuler Int     @id @default(autoincrement())
  nama_ekskul        String
  p_jwb_ekskul       String
  gambar_ekskul      String?
  deskripsi          String  @db.Text
  id_admin           Int

  admin Admin @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)

  @@index([id_admin], map: "ekstrakurikuler_id_admin_idx")
  @@map("ekstrakurikuler")
}

model Prestasi {
  id_prestasi     Int     @id @default(autoincrement())
  judul_prestasi  String
  deskripsi       String  @db.Text
  peraih_prestasi String
  gambar_prestasi String?
  id_admin        Int

  admin Admin @relation(fields: [id_admin], references: [id_admin], onDelete: Cascade)

  @@index([id_admin], map: "prestasi_id_admin_idx")
  @@map("prestasi")
}

model Pendaftar {
  id_pendaftar       Int       @id @default(autoincrement())
  nama_lengkap       String
  kata_sandi         String?
  nisn               String?   @unique
  alamat             String    @db.Text
  tempat_lahir       String?
  tanggal_lahir      DateTime? @db.Date
  jenis_kelamin      String?
  no_hp              String
  asal_sekolah       String
  email              String?
  nama_wali          String?
  status_pendaftaran String    @default("menunggu_verifikasi")
  catatan_dokumen    String?   @db.Text
  tanggal_daftar     DateTime  @default(now())
  id_gelombang       Int?

  dokumen     Dokumen[]
  gelombang   Gelombang?   @relation(fields: [id_gelombang], references: [id_gelombang])
  pengumumans Pengumuman[]

  @@index([status_pendaftaran], map: "pendaftar_status_idx")
  @@map("pendaftar")
}

model Dokumen {
  id_dokumen     Int    @id @default(autoincrement())
  nama_dokumen   String
  jenis_dokumen  String
  id_pendaftar   Int
  id_verifikator Int?

  pendaftar   Pendaftar    @relation(fields: [id_pendaftar], references: [id_pendaftar], onDelete: Cascade)
  verifikator Verifikator? @relation(fields: [id_verifikator], references: [id_verifikator], onDelete: SetNull)

  @@unique([id_pendaftar, nama_dokumen], map: "dokumen_unique")
  @@index([id_pendaftar], map: "dokumen_id_pendaftar_idx")
  @@index([id_verifikator], map: "dokumen_id_verifikator_idx")
  @@map("dokumen")
}

model Gelombang {
  id_gelombang    Int      @id @default(autoincrement())
  nama            String
  tanggal_mulai   DateTime
  tanggal_selesai DateTime
  kuota           Int

  pendaftar Pendaftar[]

  @@map("gelombang")
}
