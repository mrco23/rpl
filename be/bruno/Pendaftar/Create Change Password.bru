meta {
  name: Create Change Password
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/pendaftar/change-password
  body: json
  auth: bearer
}

body:json {
  {}
}

auth:bearer {
  token: {{token}}
}
