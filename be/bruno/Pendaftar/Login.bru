meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/pendaftar/login
  body: json
  auth: none
}

body:json {
  {
    "nisn": "0012345678",
    "password": "Maltrian123!"
  }
}

script:post-response {
  const data = res.getBody();
  if (data.token) {
    bru.setEnvVar("token", data.token);
  }
}
