meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/pendaftar/login
  body: json
  auth: none
}

body:json {
  {
    "nisn":"2026020001",
    "password":"Maltrian123!"
  }
}

script:post-response {
  let resBody = res.getBody();
  if (resBody && resBody.token) {
    bru.setEnvVar("token", resBody.token);
  }
}
