meta {
  name: Create Reset Password
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/pendaftar/reset-password
  body: json
  auth: bearer
}

body:json {
  {}
}

auth:bearer {
  token: {{token}}
}
