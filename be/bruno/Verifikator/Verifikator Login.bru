meta {
  name: Verifikator Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/verifikator/login
  body: json
  auth: none
}

body:json {
  {
    "username": "Maltrian",
    "password": "Maltrian123!"
  }
}

script:post-response {
  const data = res.getBody();
  if (data.token) {
    bru.setEnvVar("token", data.token);
  }
}
