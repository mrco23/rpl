meta {
  name: Update Verifikasi Dokumen
  type: http
  seq: 1
}

patch {
  url: {{base_url}}/dokumen/1/verifikasi
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status_verifikasi": "terima",
    "catatan_verifikasi": "Dokumen lengkap"
  }
}
