meta {
  name: Pendaftar Login
  type: http
  seq: 4
}

post {
  url: {{base_url}}/login
  body: json
  auth: none
}

body:json {
  {
    "username": "esya01",
    "password": "EsyaCantik123!"
  }
}

script:post-response {
  const data = res.getBody();
  if (data.token) {
    bru.setEnvVar("token", data.token);
  }
}
