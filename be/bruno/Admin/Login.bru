meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/admin/login
  body: json
  auth: none
}

body:json {
  {}
}

script:post-response {
  let resBody = res.getBody();
  if (resBody && resBody.token) {
    bru.setEnvVar("token", resBody.token);
  }
}
