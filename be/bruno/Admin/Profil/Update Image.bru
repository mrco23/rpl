meta {
  name: Update Image
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/profile/image
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  logo: @file(path/to/logo.png)
  foto_kepala_sekolah: @file(path/to/foto.jpg)
}
