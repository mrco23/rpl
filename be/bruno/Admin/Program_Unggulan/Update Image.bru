meta {
  name: Update Image
  type: http
  seq: 2
}

patch {
  url: {{base_url}}/program-unggulan/1/image
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  gambar: @file(path/to/image.jpg)
}
