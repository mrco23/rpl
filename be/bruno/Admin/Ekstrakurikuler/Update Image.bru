meta {
  name: Update Image
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/ekstrakurikuler/1/image
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  gambar: @file(path/to/image.jpg)
}
