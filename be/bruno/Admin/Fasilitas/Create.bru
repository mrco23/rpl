meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{base_url}}/fasilitas
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nama": "Laboratorium Komputer",
    "deskripsi": "Fasilitas lab komputer"
  }
}
