meta {
  name: Update Image
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/fasilitas/:id/image
  body: multipartForm
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  gambar: @file()
}
